# v0.18.16 — Section header alignment

UI-only correction to v0.18.15.

- Removed the boxed S / S / L section initials.
- Kept the section icons.
- SOURCES, STORAGE and LOADS now use the same fixed-width icon column so the left edge of all three text labels is vertically aligned.
- No MasterBus mappings, calculations, control logic, or safety behavior changed.
